import React from 'react'

const SingleArticle = (props) => {
    const { article } = props;
    console.log(article)
    return (
        <>
        single article
        </>
    )
}

export {SingleArticle}